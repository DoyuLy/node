//把form表单传递进去
function validateForm(form, options){
	//遍历form中的表单标签
	for (var obj in form) {
		switch case 遍历类型
		接着写各自的空值判断 和  正则

		最后抛出异常提示 return msg()
	};

	通过则提交  没通过则阻止
}

function msg(type ,options){

}